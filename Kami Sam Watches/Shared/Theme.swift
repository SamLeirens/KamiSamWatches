import SwiftUI

enum Theme {
    static let cardCornerRadius: CGFloat = 16
    static let thumbCornerRadius: CGFloat = 10
    static let cardBackground = Color("CardBackground")
    static let imagePlaceholder = Color("CardBackground")
}
